package net.minecraft.server.network;

import net.minecraft.network.Connection;

/**
 * STUB of vanilla net.minecraft.server.network.ServerGamePacketListenerImpl.
 * Compile-time only; never packaged. The real hierarchy (it extends
 * ServerCommonPacketListenerImpl) exposes the public field {@code connection}.
 */
public class ServerGamePacketListenerImpl {

    public Connection connection = null;
}
