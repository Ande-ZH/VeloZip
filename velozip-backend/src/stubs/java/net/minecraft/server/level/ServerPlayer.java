package net.minecraft.server.level;

import net.minecraft.server.network.ServerGamePacketListenerImpl;

/**
 * STUB of vanilla net.minecraft.server.level.ServerPlayer. Compile-time only;
 * never packaged. The real class declares (or inherits) the public field
 * {@code connection}; JVM field resolution walks the real hierarchy at runtime.
 */
public class ServerPlayer {

    public ServerGamePacketListenerImpl connection = null;
}
